[pytest]
# Repo-tooling tests only (the deterministic verdict/measurement engine).
# The target harness under integration-test/tests/ is generated per-target and
# runs inside a pipeline job against the built target, not in repo CI.
testpaths = tests
python_files = test_*.py
addopts = -q
