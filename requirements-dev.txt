# Development / CI dependencies for the CodeWeave repo tooling tests.
# These cover only the deterministic pipeline tools under integration-test/_tools/
# (the statistics/verdict engine), NOT the target harness dependencies, which are
# installed per-target from SOURCE-UNDER-INVESTIGATION.md during a pipeline run.
pytest>=8,<10
numpy>=1.26
scipy>=1.11
